/**
 * @format
 *  Expo entry point for the app. This file is required for Expo to run the app.
 */

import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);