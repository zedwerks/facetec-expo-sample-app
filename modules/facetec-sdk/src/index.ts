/**
 * facetec-sdk-module
 *
 * Autolinked as a native Expo module (see expo-module.config.json /
 * facetec-sdk-module.podspec) AND consumed as a regular JS package — the
 * FaceTec.ts wrapper here calls `requireNativeModule("FaceTecSDKModule")`
 * internally, so app code never touches the native module directly.
 */
export * from "./FaceTec";
